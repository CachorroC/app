{
  "permissions": {
    "allow": [
      "Bash(pkill -f \"next dev\")",
      "Bash(curl -s -m 2 http://localhost:3000/memoriales)",
      "Bash(docker --version)",
      "Bash(docker build *)",
      "Bash(docker compose *)"
    ]
  }
}

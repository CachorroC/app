import { Loader } from '#@/components/Loader';
import { Fragment } from 'react';

export default function MainLoading() {
  return (
    <Fragment>
      <Loader key={'a'} />
      <Loader key={'b'} />
      <Loader key={'c'} />
      <Loader key={'d'} />
      <Loader key={'e'} />
      <Loader key={'f'} />
      <Loader key={'g'} />
      <Loader key={'h'} />
      <Loader key={'i'} />
      <Loader key={'j'} />
    </Fragment>
  );
}

import { Loader } from 'components/Loader';

export default function Loading() {
      return (
        <>
          <Loader key={'a'} />
          <Loader key={'b'} />
          <Loader key={'c'} />
          <Loader key={'d'} />
          <Loader key={'e'} />
          <Loader key={'f'} />
          <Loader key={'g'} />
          <Loader key={'h'} />
          <Loader key={'i'} />
          <Loader key={'j'} />
        </>
      );
}
